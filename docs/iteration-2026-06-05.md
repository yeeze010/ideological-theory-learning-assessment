# 迭代记录 2026-06-05

## 本轮处理

- 审计仓库文档、CI、代码结构与可执行验证项。
- 修复 `apps/web` 的 ESLint 解析配置，使 `.vue` 文件可参与 `lint`。
- 新增标准 `docs/` 目录，整理需求、设计、接口、测试、部署、验收与仓库规范。
- 补齐 GitHub Issue、PR、Code Owners 模板。

## 本地验证结果

| 命令 | 结果 |
| --- | --- |
| `cmd /c npm test` | 通过 |
| `cmd /c npm run build` | 通过 |
| `cmd /c npm run typecheck` | 通过 |
| `cmd /c npm run lint` | 修复配置后需复验 |

## 发现的问题

1. API 仍以内存数据为主，尚未形成真实数据库业务闭环。
2. Prisma schema 未覆盖角色、权限、学习进度、成绩、证书、通知、文件与审计全量模型。
3. 前端路由仅做登录态拦截，未落地角色菜单和数据范围控制。
4. GitHub 工程模板此前缺失，已在本轮补齐。
5. 当前自动化仅对本仓库有写权限，其他 16 个项目需要后续分别进入对应仓库处理。

## 下一步

1. 把 API 种子数据迁移到 Prisma + PostgreSQL。
2. 落地 RBAC、组织数据范围和审计拦截器。
3. 为课程学习、考试提交、报表查询补接口自动化测试。
4. 补前端角色菜单、空状态、异常状态和验收导出流程。
5. 进入其余 16 个项目逐仓执行同样的审计与补齐动作。
